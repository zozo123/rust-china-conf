---
marp: true
title: 百万次编译，一小时机器人
paginate: true
style: |
  section { background: #10171d; color: #edf2f4; font-family: "Noto Sans SC", "PingFang SC", sans-serif; font-size: 28px; }
  h1, h2 { color: #edf2f4; }
  a { color: #f0b85d; }
  table { font-size: 24px; }
---

# 百万次编译，一小时机器人

临时工作区与独立核验的机器人行为

[English](slides.en.md)

<!-- 标题表达动机，不是已测得的换算关系。25分钟演讲含六分钟演示，另留五分钟问答。 -->

---

## 机器人成功了

![bg right:42% contain](../assets/robot-lift-poster.png)

| 检查 | 植入缺陷版本的记录 |
| --- | --- |
| 方块举起 | 是 |
| 观测年龄 | 600 ms |
| 时效契约 | 未通过 |

<!-- 图片来自另一个新鲜观测回合，用于说明仿真任务。缺陷决策数据请展示植入版本回放。来源：../examples/ec2-runner-a/events-stale_600ms.jsonl -->

---

## Rust 契约

1. 仿真停止信号优先
2. 未来观测时间戳无效
3. 每段派发前，观测年龄须 ≤ 250 ms

植入的实现缺少第 3 项检查。

<!-- 边界测试覆盖250/251 ms。这是演示策略，授权发生在运动段边界，不是每个控制步。 -->

---

## 智能体改动与验收标准

| 智能体可以提出 | 候选补丁不能修改 |
| --- | --- |
| 决策门实现补丁 | 契约测试 |
| 说明与测试输出 | 仿真器及场景 |
| | 校验器与运行器脚本 |

默认彩排使用已审阅的回退补丁。

<!-- 仓库脚本不会调用LLM。如果现场使用外部智能体，应单独展示该尝试，并明确标识回退。 -->

---

## 候选验证技术栈

| 组件 | 职责 |
| --- | --- |
| 临时工作区 | 候选源码与全新输出 |
| Incredibuild | 接入分布式编译与复用能力 |
| Rust + Python | 决策协议与仿真执行 |
| 受保护校验器 | 必需场景、时序轨迹、产物身份 |

<!-- IB预期收益需要辅助机和缓存遥测支持。仅构建路径运行不能证明缓存复用。 -->

---

## 工作区生命周期

A 导出失败证据与基线修订。

B 应用候选补丁，使用全新输出重建。

**当前使用 EC2 工作区，计划接入 islo。**

<!-- 本地Git工作区不是虚拟机安全边界。删除的是工作区，不是EC2主机。测试重新执行。 -->

---

## 六分钟演示

复现缺陷、审查补丁、删除 A

B 重新构建、拒绝陈旧请求、完成新鲜任务

展示实际可执行文件的证据摘要

<!-- 切到舞台手册。区分回放与实时结果。不能把彩排17场景覆盖当作现场两个精选用例刚完成的结果。 -->

---

## 修复后的行为

| 观测 | 任务派发 | 结果 |
| --- | --- | --- |
| 陈旧，600 ms | 0 | tick 12 拒绝 |
| 新鲜，0 ms | 4 | 方块举起 |

来自 Linux 记录中同一个修复后的可执行文件

<!-- 来源：../examples/ec2-runner-b/events-center-f600.jsonl 和 events-center-f0.jsonl -->

---

## 构建观测

| 历史阶段 | 耗时 |
| --- | --- |
| 运行器 A | 21.426 s |
| 运行器 B | 21.948 s |

B **慢了 522 ms**，没有测得加速。

<!-- 两个记录均标明IB。候选不同，每阶段只有一个样本，缓存状态未受控。指标未证明辅助机执行或缓存复用。 -->

---

## 证据摘要

17 个已记录的仿真回合：

**10 次举起、5 次陈旧拒绝、1 次停止、1 次超时**

源码、补丁、可执行文件身份与事件轨迹

<!-- 运行编号ec2-e2e-20260923-160725。原校验器报告88/88，当前加强后的检查范围不同。历史二进制未提交，新彩排导出自己的产物。摘要标识身份，不认证执行历史。 -->

---

## 已演示的范围

软件在环，按运动段授权

方块位置来自仿真器状态

实体硬件与性能收益需要独立验证

<!-- 同样未覆盖训练视觉、逐控制步连续监督和islo执行。简洁说明。 -->

---

## 每个候选都要重新检查

临时执行与可复用编译具有不同的生命周期。

机器人实际行为决定修复是否满足契约。

[源码、回放与证据](https://github.com/zozo123/rust-china-conf)

<!-- 收尾后问答。受控基准：同一固定候选，原生/IB空缓存/IB父修订预热，每模式至少五次，中位数和范围，披露竞争负载。 -->
